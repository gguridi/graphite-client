language: go

go:
  - 1.8
  - 1.9
  - "1.10"
  - 1.11
  - tip

before_install:
- go get github.com/onsi/gomega
- go get github.com/onsi/ginkgo/ginkgo

script:
  - ginkgo -r --randomizeAllSpecs --failOnPending --randomizeSuites --cover --trace --race -outputdir=./ ./...
