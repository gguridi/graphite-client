language: go

go:
  - "1.10"
  - 1.11
  - 1.12
  - tip

before_install:
- go get github.com/onsi/ginkgo/ginkgo
- go get github.com/onsi/gomega/...

script:
  - ginkgo -r --cover --trace --race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)
